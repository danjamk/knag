# The prompt

Paste into the Claude Design session. It has the repo, so this points rather than explains;
`docs/design/phase-8-brief.md` is the brief it refers to.

---

Four small asks this time, not a feature and not a holistic pass. The brief is
**`docs/design/phase-8-brief.md`**, and §1 is what shipped while you were away — read it
first, because two of your earlier rulings have dated amendments now (§4 the ledge, §7e
the settings labels) and both came from a week of using the thing on a phone.

Every ask is already built up to the point where your answer is the missing piece. I need
rulings, not a bundle:

1. **§2, #197 — two numbers.** The ledge reads like a footnote on a phone at text size
   20: 18px glyphs over 11px labels. Ledge label size and ledge glyph size, applied to
   `.ledge-item` and nothing else. My proposal is 13px and 20px; 56px holds it. Your §3b
   rule — chrome does not follow the reading preference — is right for the bar, and the
   ledge is the one surface it does not have to bind, because the ledge is never open with
   the keyboard up. If you hold the line instead, say why.

2. **§3, #196 — a dev mark.** Two installs on one iPad were identical tiles both called
   `knag`; the name is fixed now, the tile is not. One signal that reads as "same product,
   not the real one" at 16px and 180px. Deliverables mirror the prod set exactly. Prod
   stays as it is.

3. **§4, #190 — where the operator's instructions are edited.** The setting exists on the
   server and reaches every agent conversation; it needs a surface. A textarea collides
   with §7e twice — a preference has a current value, and devices is the only chevron. An
   `agent ›` row under `you`, a way in from manage-pages, or something else. Which, and
   the copy. Phone, textarea under the keyboard, is the case.

4. **§5, #195 — the drag affordance in manage-pages.** Order is server-side and works
   through a route; it needs a control in a settings pane that is not Arrange, on rows
   that already carry a text field. Grip, long-press, or up/down for a list of nine.
   Which.

🔴 Mobile-first, tokens we can apply in an afternoon, amber the only colour, one animation
— unchanged. The build applies the number you give, not the one I wrote.
